drop function if exists public.create_checkout_order(
  uuid,
  uuid,
  text,
  text,
  numeric,
  text,
  text,
  jsonb,
  jsonb
);

drop function if exists public.create_checkout_order(
  uuid,
  uuid,
  text,
  text,
  text,
  text,
  jsonb,
  jsonb
);
